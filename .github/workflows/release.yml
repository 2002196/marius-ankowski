name: Release (smoke test)

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - name: Smoke
        shell: bash
        run: |
          set -euo pipefail
          echo "Release workflow parsed & running: ${GITHUB_REF}"
